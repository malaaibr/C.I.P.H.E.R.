# Core

Shared contracts, schemas, and protocol-facing abstractions for the local MVP.

Planned contents:

- A2A task envelopes
- agent-card models
- memory and tool client contracts
- shared identifiers and trace metadata

