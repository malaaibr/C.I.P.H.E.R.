# Local Deploy

Placeholder location for local MVP deployment assets such as:

- Docker Compose definitions
- local bootstrap scripts
- service configuration for SQLite, Memgraph, Qdrant, Redis, and Langfuse
